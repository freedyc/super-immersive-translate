// Curated Lucide icon set — only the icons actually used across the extension.
// Importing the full `icons` object from 'lucide' pulls in the entire ~1500-icon
// library (~400KB per page). Listing the specific icons here lets the bundler
// tree-shake everything else.
//
// When you introduce a new lucide icon in any page, add its PascalCase export here
// (e.g. the icon named arrow-left → ArrowLeft) and include it in the object below.
import {
  ArrowLeft, ArrowLeftRight, ArrowRight, BarChart2, BookOpen, Check, CheckCircle,
  CloudSync, Copy, Database, Download, ExternalLink, File, FileText, Globe, Globe2, HelpCircle,
  History, Image, Inbox, Info, Keyboard, Languages, Layers, List, Mic, Monitor, Moon,
  MousePointer2, Package, PackageOpen, Palette, PanelRight, PenLine, Plus, RotateCcw, RotateCw, SearchX,
  Settings, Settings2, Share2, Shuffle, SkipForward, Star, Sun, Trash2, Upload,
  Volume2, X, Zap,
} from 'lucide';

export const icons = {
  ArrowLeft, ArrowLeftRight, ArrowRight, BarChart2, BookOpen, Check, CheckCircle,
  CloudSync, Copy, Database, Download, ExternalLink, File, FileText, Globe, Globe2, HelpCircle,
  History, Image, Inbox, Info, Keyboard, Languages, Layers, List, Mic, Monitor, Moon,
  MousePointer2, Package, PackageOpen, Palette, PanelRight, PenLine, Plus, RotateCcw, RotateCw, SearchX,
  Settings, Settings2, Share2, Shuffle, SkipForward, Star, Sun, Trash2, Upload,
  Volume2, X, Zap,
};
